namespace Cpsc370Final.Tests;

public class UnitTest1
{
    /*
    [Fact]
    public void GameShouldEndWhenLivesRunOut()
    {
        // Arrange: Set up the game with a known word and initial lives
        string testWord = "apple";
        PlayerTurns.SetupGame(testWord);

        // Simulate incorrect guesses
        GuessManager.GuessLetter("z");  // Incorrect guess
        GuessManager.GuessLetter("x");  // Incorrect guess
        GuessManager.GuessLetter("q");  // Incorrect guess
        GuessManager.GuessLetter("b");
        GuessManager.GuessLetter("c");
        GuessManager.GuessLetter("d");
        GuessManager.GuessLetter("r");
        GuessManager.GuessLetter("y");
        GuessManager.GuessLetter("t");
        GuessManager.GuessLetter("f");

        // Act: Check if the game is over (it should be a loss)
        bool result = WinChecker.IsGameOver();

        // Assert: The game should end with a loss (because lives run out)
        Assert.True(result);
    }
    */

}